version: 0.2

phases:
    install:
        commands:
            - echo Entered the install phase
            - apt-get update -y
    pre_build:
        commands:
            - jenkinCICD/java-tomcat-sample/pom.xml
            - echo Entered the pre_build phase..
    build:
        commands:
            - mvn --version
            
            - mvn clean install -f pom.xml
    post_build:
        commands:
            - echo Entered the post_build phase..
